(function () {
    // Define the number whose square needs to be calculated
    let number = 5;

    // Calculate the square of the number
    let square = number * number;

    // Display the result
    console.log("Square of", number, "is", square);
})();
